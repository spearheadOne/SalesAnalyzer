# Sales Cleanup

Tiny lambda function to clean up sales data bucket by cron job.

## Run locally
```
../gradlew run
```

## Note
It is run on aws as a native runtime image. If you want to run it locally like this use graalvm