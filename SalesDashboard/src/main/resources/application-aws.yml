mybatis:
  environment: aws

datasources:
  default:
    url: jdbc:postgresql://${RDS_ENDPOINT}:5432/metrics

aws:
  rds:
    secret-id: ${RDS_CREDENTIALS_ARN}
  services:
    sqs:
      queueUrl: ${SQS_QUEUE_URL}